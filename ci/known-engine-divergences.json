{
  "_README": [
    "Divergences between the MySQL and DuckDB execution engines that are ACCOUNTED",
    "FOR, so the engine A/B gate can fail on the ones that are not.",
    "",
    "This baseline is separate from duck/known-divergences.json in the rvf repo,",
    "and started empty. That one classifies a Python engine against a production",
    "MySQL report - different corpus, different host, different release. This one",
    "classifies two instances of ONE jar, on one host, validating one release,",
    "with only rvf.execution.engine differing. The causes are not the same, so",
    "inheriting its allowances would import excuses that do not apply.",
    "",
    "An entry is a claim that a difference is UNDERSTOOD, not that it is",
    "acceptable. Each needs a direction, what it applies to, the cause, and the",
    "evidence for that cause - a file and line, a measurement, or a reproduction.",
    "'Both engines have always done this' is not evidence.",
    "",
    "The point is for this file to shrink. An entry removed because the underlying",
    "defect was fixed belongs in removedAllowances with the commit that fixed it,",
    "so nobody re-adds it.",
    "",
    "Keys are assertion UUIDs, because that is what the gate looks up. The slug field carries the human name.",
    "",
    "REMOVED 2026-09-10: `26c25479` / duckdb-reports-not-run-where-mysql-false-passes.",
    "It described release-type-snapshot-owl-expression-successive-states reporting",
    "not-run while MySQL answered 0 against a dependency that was not supplied. The",
    "statement's only use of the dependency was an anti-join requiring NULL - a",
    "no-op against an empty relation - so DuckDB now runs it and both engines",
    "agree. Build 16337 listed the entry as no longer diverging, which is what",
    "removing it is for. See duck/PLAN.md 3.14.",
    "",
    "If MySQL's PR #6 lands and it stops ERRORING on",
    "file-centric-snapshot-inactivated-component-module, that assertion's 28",
    "statements DO compare against the dependency, so MySQL would answer against",
    "an empty schema where DuckDB still declines. That would be a new divergence",
    "of this shape, and it should be classified then, with the numbers from the",
    "run that shows it - not pre-registered here on a guess."
  ],
  "directions": {
    "candidate-higher": "DuckDB reports more failures than MySQL for an assertion",
    "incumbent-higher": "MySQL reports more failures than DuckDB for an assertion"
  },
  "divergences": {
    "02cf4438-170e-4b6b-b5da-624ef730a8e0": {
      "direction": "candidate-higher",
      "appliesTo": "file-centric-snapshot-inactivated-component-module, on any release that ships an Identifier file",
      "assertionUuids": [
        "02cf4438-170e-4b6b-b5da-624ef730a8e0"
      ],
      "observed": "MySQL failureCount -1 in assertionsWarning; DuckDB 0 in assertionsPassed",
      "cause": [
        "The assertion calls validate_inactivated_component_module, whose cursor is",
        "'select table_name from information_schema.tables ... and table_name like",
        "'%\\_d'' - every delta table in the schema - and the dynamic SQL it builds",
        "selects t1.id.",
        "",
        "identifier_d has no id column. RF2 identifies those rows by",
        "alternateidentifier, and the DDL declares",
        "(identifierschemeid, alternateidentifier, effectivetime, active, moduleid,",
        "referencedcomponentid). So MySQL raises",
        "'Unknown column t1.id in field list' and the whole assertion is abandoned.",
        "",
        "The DuckDB engine does not iterate at runtime: publish_store.py unrolls the",
        "procedure into one statement per table, and the unroller enumerated the 52",
        "tables that actually have an id. identifier_d is not among them, so the",
        "assertion runs over the tables it can answer for."
      ],
      "evidence": [
        "MySQL report failureMessage: 'Error executing SQL command object Nested",
        "exception : java.sql.SQLSyntaxErrorException: Unknown column t1.id in field list'",
        "",
        "scripts/resource/file-centric-inactivated-component-module-validation-proc.sql:23-26",
        "declares the cursor; :78 selects t1.id.",
        "",
        "The store's unrolled form of 02cf4438 holds 56 statements over 52 tables and",
        "does not name identifier_s or identifier_d - checked directly against",
        "src/main/resources/duck/store.json.",
        "",
        "identifier_d exists in MySQL only because create-tables-mysql.sql now creates",
        "it: it is one of the 12 tables added by the RF2-file-types change (fork PR #23,",
        "IHTSDO #74). Before that change the cursor never saw it."
      ],
      "notThePlan": [
        "FIXED, pending the corpus change landing. IHTSDO/snomed-release-validation-assertions#6",
        "adds an exists clause on information_schema.columns so the cursor yields only",
        "tables the procedure can query. Verified against the AU edition: MySQL then",
        "executes cleanly with 0 findings, agreeing exactly with DuckDB's 0, and",
        "identifier_d is the only table filtered out.",
        "",
        "This entry stays only until that PR merges and the corpus this repo pins moves",
        "past it. At that point it belongs in removedAllowances - the gate should start",
        "failing if the divergence reappears."
      ],
      "slug": "mysql-errors-on-identifier-having-no-id-column",
      "directionNote": "MySQL -1 against DuckDB 0, so the tool reads it as candidate-higher. The arithmetic is on the sentinel, not on findings: MySQL abandoned the assertion and DuckDB executed it and found nothing.",
      "class": "mysql-errors-on-identifier-having-no-id-column"
    }
  },
  "coverage": {
    "_README": [
      "Assertions one engine reports that do not join to the other by assertionUuid.",
      "Zero is the target: both engines read the same corpus, so a missing join is",
      "an assertion one of them silently did not run."
    ],
    "allowUncovered": 0,
    "categories": {}
  },
  "removedAllowances": [
    {
      "pending": "02cf4438-170e-4b6b-b5da-624ef730a8e0",
      "fixedBy": "IHTSDO/snomed-release-validation-assertions#6",
      "moveWhen": "the pinned assertion corpus includes that commit",
      "evidence": "MySQL executes the assertion cleanly with 0 findings, matching DuckDB"
    }
  ]
}

--- a/rvfsql.py	2026-08-26 10:29:17.000000000 +1000
+++ b/rvfsql.py	2026-09-09 22:19:51.955222150 +1000
@@ -11,6 +11,7 @@
                       instead of being rewritten per script.
 """
 import re
+import sys
 
 import procedures
 
@@ -406,6 +407,40 @@
     return changed
 
 
+def _unicode_posix_classes(tree):
+    """Rewrite POSIX classes in every regex pattern the tree carries.
+
+    An AST pass, not a textual one, because sqlglot - not `_regexp_to_duckdb` -
+    is what converts `x REGEXP 'p'` for a statement it can parse, so a textual
+    pass over the input never sees the pattern that reaches DuckDB.
+
+    Both node shapes are handled: sqlglot's own `RegexpLike`, and the
+    `regexp_matches` Anonymous that `_case_insensitive_on_function_results`
+    produces - so this pass is order-independent with respect to it.
+    """
+    from sqlglot import exp
+
+    def rewrite(literal):
+        if not isinstance(literal, exp.Literal) or not literal.is_string:
+            return False
+        translated = unicode_posix_classes(literal.this)
+        if translated == literal.this:
+            return False
+        literal.set("this", translated)
+        return True
+
+    changed = False
+    for node in list(tree.find_all(exp.RegexpLike)):
+        changed = rewrite(node.expression) or changed
+    for node in list(tree.find_all(exp.Anonymous)):
+        if str(node.this).lower() != "regexp_matches":
+            continue
+        args = node.expressions
+        if len(args) >= 2:
+            changed = rewrite(args[1]) or changed
+    return changed
+
+
 def _fix_loose_group_by(tree):
     import sqlglot
     from sqlglot import exp
@@ -524,9 +559,53 @@
     return "".join(out)
 
 
+# MySQL 8's regex is ICU, whose POSIX classes are UNICODE-aware; DuckDB's is
+# RE2, whose POSIX classes are ASCII-ONLY. Measured on the AU edition's 1,826,331
+# active terms, `[[:alnum:]]+/[[:alnum:]]+` selects 76,165 rows on MySQL and
+# 76,164 on DuckDB - the missing one being "History of - vertigo/Meniere
+# disease", whose accented letter RE2 does not count as alphanumeric.
+#
+# One row today, but the shape is a silent blind spot: an assertion that checks
+# term formatting stops seeing accented content, reports nothing, and reads as a
+# pass. `[\p{L}\p{N}]` restores it exactly - 76,165, including that row.
+#
+# Only the classes the corpora actually use are translated. An untranslated one
+# is REFUSED rather than passed through, because passing it through is the
+# failure this comment describes.
+_POSIX_TO_RE2 = {
+    "alnum": r"\p{L}\p{N}",
+    "alpha": r"\p{L}",
+    "digit": r"\p{Nd}",
+    "lower": r"\p{Ll}",
+    "upper": r"\p{Lu}",
+}
+_POSIX_CLASS_RE = re.compile(r"\[:([a-z]+):\]")
+
+
+def unicode_posix_classes(pattern: str) -> str:
+    """ICU's Unicode-aware POSIX classes, as RE2 property classes.
+
+    Applied to the pattern INSIDE its brackets: `[[:alnum:]]` is a bracket
+    expression containing the class, so the replacement is the property set
+    without its own brackets and the surrounding ones are kept - which also
+    makes `[[:alnum:]_-]` come out right.
+    """
+    def repl(m):
+        name = m.group(1)
+        replacement = _POSIX_TO_RE2.get(name)
+        if replacement is None:
+            raise ValueError(
+                f"POSIX class [:{name}:] has no verified RE2 equivalent. "
+                "MySQL's ICU classes are Unicode-aware and RE2's are ASCII-only, "
+                "so passing it through would silently narrow the match. Add it to "
+                "_POSIX_TO_RE2 with a measurement.")
+        return replacement
+    return _POSIX_CLASS_RE.sub(repl, pattern)
+
+
 def _regexp_to_duckdb(m):
     col, negate, lit = m.group(1), m.group(2), m.group(3)
-    decoded = decode_mysql_string(lit[1:-1])
+    decoded = unicode_posix_classes(decode_mysql_string(lit[1:-1]))
     call = f"regexp_matches({col}, '{decoded.replace(chr(39), chr(39) * 2)}')"
     # `term not regexp '...'` must become NOT regexp_matches(term, '...'), not
     # regexp_matches(not, '...') - the old pattern captured `not` as the column.
@@ -575,7 +654,102 @@
     return pattern.sub(repl, sql)
 
 
-def to_duckdb(sql: str) -> str:
+_NUMERIC_TYPE_PREFIXES = ("bigint", "int", "smallint", "tinyint", "mediumint",
+                          "decimal", "numeric", "double", "float", "real")
+
+# `numcol = ''` / `numcol <> ''`, qualified or not. Deliberately anchored on the
+# empty-string literal: a non-empty literal compared to a numeric column is a
+# different question (MySQL would parse a leading number out of it), and no
+# assertion in either corpus does that.
+_EMPTY_STRING_CMP_RE = re.compile(r"\b((?:\w+\.)?(\w+))\s*(=|!=|<>)\s*''")
+
+
+def numeric_column_names(table_columns):
+    """Column names the DDL declares numeric in EVERY table that has them.
+
+    Takes the TYPE-TEXT form - {table: "id BIGINT, mapgroup SMALLINT"} - and
+    raises on the name-only form, because there is a map of each shape in this
+    codebase and they are interchangeable by accident: publish_store's
+    column_names() gives {table: {names}} for procedure unrolling, and its
+    table_columns() gives the type text. Handed the wrong one, a tolerant
+    implementation would find no numeric columns, rewrite nothing, and leave the
+    two assertions this exists to fix erroring exactly as before - a silent
+    no-op, which is the failure mode worth being noisy about.
+
+    Name-keyed rather than table-keyed on purpose. Resolving `a.mapGroup` to a
+    table means alias scope analysis, and RF2 column names are globally
+    type-consistent - mapgroup is SMALLINT wherever it appears. The all() is
+    what keeps that honest: a name declared numeric in one table and VARCHAR in
+    another is left alone rather than guessed at.
+    """
+    types = {}
+    for table, spec in (table_columns or {}).items():
+        if not isinstance(spec, str):
+            raise TypeError(
+                f"numeric_column_names needs the type-text column map, got "
+                f"{type(spec).__name__} for {table}. Pass table_columns(ddl), "
+                f"not column_names(ddl).")
+        for col in spec.split(","):
+            parts = col.strip().split(None, 1)
+            if len(parts) == 2:
+                types.setdefault(parts[0].lower(), set()).add(
+                    parts[1].split("(")[0].strip().lower())
+    return {name for name, seen in types.items()
+            if all(t.startswith(_NUMERIC_TYPE_PREFIXES) for t in seen)}
+
+
+def _numeric_vs_empty_string(sql: str, numeric_columns) -> str:
+    """`mapGroup = ''` -> `mapGroup = 0` where mapGroup is numeric.
+
+    MySQL coerces the empty string to 0 and compares; DuckDB refuses to cast it
+    and the statement dies with "Could not convert string '' to INT16" - per
+    row, so an empty table raises nothing and a populated one fails.
+
+    That cost two assertions silently. component-centric-snapshot-complexmap-
+    group.sql and its extendedmap twin ask `mapGroup IS NULL OR mapGroup = ''`
+    against a SMALLINT column; they errored on every release carrying complexmap
+    rows, and the AU nightly has none, so both reported zero findings and read
+    as passes. AssertionCorpusDigestTest found them against a fixture that does
+    have rows.
+
+    Rewriting rather than leaving it to the corpus: `= ''` is valid MySQL and
+    the corpus is upstream's. The blank it looks for loads as 0 through MySQL's
+    loader, so 0 is what the assertion means.
+    """
+    if not numeric_columns:
+        return sql
+
+    def rewrite(m):
+        return (f"{m.group(1)} {m.group(3)} 0"
+                if m.group(2).lower() in numeric_columns else m.group(0))
+
+    return _EMPTY_STRING_CMP_RE.sub(rewrite, sql)
+
+
+# A statement whose tail is transaction control, because the script before it
+# omitted its semicolon: `insert ... ) commit`. A STANDALONE commit is dropped
+# by the split loops below, but a glued one is part of the statement and DuckDB
+# fails to parse it. One assertion in the international corpus is written this
+# way - release-type-snapshot-delta-validation-expression-association-refset.sql
+# - and it is the only statement in the whole 360-assertion store containing the
+# token, so nothing detected it until a fixture had rows in the table.
+#
+# Stripped rather than refused: the token is meaningless in DuckDB, which
+# autocommits, and refusing would drop the assertion from the store, which the
+# corpus gate would then read as a missing assertion rather than a malformed
+# script. The upstream fix is the missing semicolon.
+_TXN_TAIL_RE = re.compile(r"[\s;]*\b(commit|rollback|begin|start\s+transaction)\s*$", re.I)
+
+
+def strip_transaction_tail(stmt: str):
+    """(statement, stripped_token or None)."""
+    m = _TXN_TAIL_RE.search(stmt)
+    if not m or m.start() == 0:
+        return stmt, None
+    return stmt[:m.start()].rstrip(), m.group(1)
+
+
+def to_duckdb(sql: str, numeric_columns=None) -> str:
     """Textual pre-passes applied before the sqlglot AST stage.
 
     Order matters: BINARY/COLLATE are stripped FIRST, because they can sit
@@ -607,6 +781,7 @@
     sql = _ENGINE_RE.sub("", sql)
     sql = _DISPLAY_WIDTH_RE.sub(r"\1", sql)
     sql = _EFFECTIVETIME_NOW_RE.sub(r"\1 \2 strftime(now(), '%Y%m%d')", sql)
+    sql = _numeric_vs_empty_string(sql, numeric_columns)
     return sql
 
 
@@ -635,6 +810,7 @@
         _having_without_group_by(tree)
         _fix_loose_group_by(tree)
         _case_insensitive_on_function_results(tree)
+        _unicode_posix_classes(tree)
     try:
         return ";\n".join(t.sql(dialect="duckdb") for t in trees)
     except Exception:
@@ -670,7 +846,7 @@
 
 
 def prepare(raw_sql: str, dialect: str, known_tables=None,
-            table_columns=None, **kw):
+            table_columns=None, numeric_columns=None, **kw):
     """Full pipeline -> list of executable statements for the given dialect.
 
     The single-phase path, kept as the reference implementation: precompile()/
@@ -684,11 +860,12 @@
     if dialect == "duckdb":
         if known_tables:
             sql = strip_schema_qualified_aliases(sql, known_tables)
-        sql = to_duckdb(sql)
+        sql = to_duckdb(sql, numeric_columns)
     stmts = []
     source = (split_with_delimiters(sql) if _DELIMITER_RE.search(sql)
               else split_statements_routine_aware(sql))
     for s in source:
+        s, txn = strip_transaction_tail(s)
         low = s.strip().lower()
         if not low:
             continue
@@ -856,7 +1033,8 @@
     return sql
 
 
-def precompile(raw_sql: str, known_tables=None, table_columns=None):
+def precompile(raw_sql: str, known_tables=None, table_columns=None,
+               numeric_columns=None):
     """Stage 1-3 with no run-specific input -> DuckDB statements with sentinels.
 
     Run once per assertion at PUBLISH time. The result is stable for the lifetime
@@ -879,11 +1057,16 @@
     if known_tables:
         sql = strip_schema_qualified_aliases(sql, known_tables,
                                              schema_names=_SENTINEL_SCHEMAS)
-    sql = to_duckdb(sql)
+    sql = to_duckdb(sql, numeric_columns)
     stmts = []
     source = (split_with_delimiters(sql) if _DELIMITER_RE.search(sql)
               else split_statements_routine_aware(sql))
     for s in source:
+        s, txn = strip_transaction_tail(s)
+        if txn:
+            # Not silent: the script is malformed and upstream should fix it.
+            print(f"rvfsql: stripped a trailing {txn.upper()} from a statement - "
+                  f"the script is missing a semicolon before it", file=sys.stderr)
         low = s.strip().lower()
         if not low:
             continue
--- a/publish_store.py	2026-08-26 10:29:17.000000000 +1000
+++ b/publish_store.py	2026-09-09 22:57:53.216575107 +1000
@@ -29,6 +29,7 @@
 import json
 import pathlib
 import re
+import subprocess
 import sys
 
 import manifest
@@ -36,6 +37,7 @@
 import procedures
 import rvfsql
 
+PREREQUISITES_SQL = "pre-requisites.sql"
 FORMAT_VERSION = 1
 
 # pre-requisites.sql materialises tables the DDL does not declare. They are real
@@ -98,6 +100,15 @@
           derive_uuids=True):
     tables = known_tables(ddl_path, prereq_dir)
     tcols = column_names(ddl_path)
+    # The TYPE-text map, not tcols. Two assertions compare a SMALLINT mapGroup
+    # to '' - valid MySQL, which coerces the literal to 0 - and DuckDB refuses
+    # the cast per row, so both errored on every release with complexmap rows
+    # and reported zero findings on the AU releases without them. Rewriting that
+    # needs to know which columns are numeric, which only this map carries.
+    numerics = rvfsql.numeric_column_names(table_columns(ddl_path))
+    if not numerics:
+        raise SystemExit("the DDL yielded no numeric columns - the '' rewrite "
+                         "would silently do nothing")
 
     static = manifest.index_by_basename(manifest.load_scripts(manifest_root))
     derived = manifest.load_amtv4(scripts_dir, group_id, category)
@@ -111,6 +122,13 @@
     # doing it. DuckStore then reads that as an empty corpus, and an empty
     # corpus reports no findings and PASSES.
     for path in sorted(scripts_dir.rglob("*.sql")):
+        if path.name.lower() == PREREQUISITES_SQL:
+            # Declared in the manifest so the INCUMBENT engine builds the *_active
+            # views - RVF's importer special-cases this exact filename - but it is
+            # not an assertion, and on this path the ports below stand in for it.
+            # Transpiling it as one would put MySQL DDL (DELIMITER, MyISAM) in the
+            # store under a uuid RVF also uses for the real prerequisite.
+            continue
         raw = path.read_text(errors="replace")
         entry = static.get(path.name)
         if entry is None and derive_uuids:
@@ -128,7 +146,8 @@
         uuid = entry["uuid"] if isinstance(entry, dict) else entry
         try:
             stmts = rvfsql.precompile(raw, known_tables=tables,
-                                      table_columns=tcols)
+                                      table_columns=tcols,
+                                      numeric_columns=numerics)
         except Exception as e:                                  # noqa: BLE001
             failures.append((path.name, f"{type(e).__name__}: {e}"))
             continue
@@ -154,7 +173,8 @@
                 "file": path.name,
                 "sha256": hashlib.sha256(raw.encode()).hexdigest()[:16],
                 "statements": rvfsql.precompile(raw, known_tables=tables,
-                                               table_columns=tcols),
+                                               table_columns=tcols,
+                                               numeric_columns=numerics),
             })
         except Exception as e:                                  # noqa: BLE001
             failures.append((path.name, f"{type(e).__name__}: {e}"))
@@ -215,8 +235,17 @@
     """
     import amtv4
     schema = rvfsql._SENTINEL_BY_PLACEHOLDER["<PROSPECTIVE>"]
-    out = [f"CREATE OR REPLACE TABLE {schema}.dual AS SELECT 1 AS dummy",
-           amtv4.TRANSITIVE_CLOSURE_SQL.format(s=schema).strip()]
+    # DUCKDB_PRELUDE first. rvf_duck.py executes it per connection, so the
+    # Python runtime gets substring_index whether or not the store carries it;
+    # the JAVA engine has no such channel - store["ports"] is the ONLY setup it
+    # applies. Omitting it published a store on which four assertions died with
+    # "Scalar Function with name substring_index does not exist":
+    # complexmap-ifa-rule, extendedmap-ifa-rule, description-fsn-synonym-text
+    # and description-unique-preferred-terms, all of which call
+    # SUBSTRING_INDEX(a.mapRule, ' ', 2) or the equivalent over a term.
+    out = [s.strip() for s in rvfsql.DUCKDB_PRELUDE.split(";") if s.strip()]
+    out.append(f"CREATE OR REPLACE TABLE {schema}.dual AS SELECT 1 AS dummy")
+    out.append(amtv4.TRANSITIVE_CLOSURE_SQL.format(s=schema).strip())
     body = re.sub(r"--[^\n]*", "", amtv4.macros(schema))
     out.extend(s.strip() for s in body.split(";") if s.strip())
     # The corpus's own scalar CREATE FUNCTIONs, which the routine-definition
@@ -225,6 +254,77 @@
     return out
 
 
+def pack_identity(name, version, corpus_root, assertions, prerequisites):
+    """This store's own identity: what it is, and what it was built from.
+
+    The bundled store reported as `bundled` with no version and no digest, so
+    nothing could be required of it and a report could not say which assertions
+    produced it. Both fields are derived, not stamped:
+
+    * VERSION is the assertion corpus's own commit date. A rebuild of unchanged
+      inputs therefore produces the same version - a wall-clock stamp would give
+      the same corpus a new identity on every build - and dates are orderable,
+      which a "requires at least" comparison needs and a commit sha cannot give.
+    * DIGEST is over the (uuid, source hash) pairs, sorted. It answers "the same
+      assertions, from the same text", independent of JSON key order, publisher
+      formatting, or which sqlglot compiled it.
+
+    Refuses to invent a version. A store whose identity is guessed is worse than
+    one with none: 3.3 refuses pack combinations by comparing these.
+    """
+    ref = date = ""
+    if version:
+        date = version
+    else:
+        try:
+            out = subprocess.run(
+                ["git", "-C", str(corpus_root), "log", "-1",
+                 "--format=%H %cd", "--date=format:%Y.%m.%d"],
+                capture_output=True, text=True, timeout=30)
+            if out.returncode == 0 and out.stdout.strip():
+                ref, date = out.stdout.strip().split(" ", 1)
+        except Exception:                                        # noqa: BLE001
+            pass
+    if not date:
+        raise SystemExit(
+            f"FATAL: no --pack-version given and {corpus_root} is not a git "
+            "checkout, so the corpus has no date to take a version from. Pass "
+            "--pack-version explicitly rather than publishing a store nothing "
+            "can be required of.")
+    # Over everything the store would EXECUTE, not only its assertions: the
+    # prerequisites build the tables every assertion reads, so a store with a
+    # changed pre-requisites.sql runs differently while its assertion set is
+    # untouched. A digest that cannot see that cannot answer "would this
+    # reproduce the old report".
+    material = "\n".join(f"{uuid}\t{a['sha256']}"
+                          for uuid, a in sorted(assertions.items()))
+    material += "\n--\n" + "\n".join(
+        f"{p['file']}\t{p['sha256']}" for p in sorted(prerequisites,
+                                                     key=lambda x: x["file"]))
+    digest = hashlib.sha256(material.encode()).hexdigest()
+    return {"name": name, "version": date, "corpusRef": ref,
+            "digest": "sha256:" + digest, "assertions": len(assertions)}
+
+
+def requirements(specs):
+    """`PACK:atLeast=VERSION` or `PACK:digest=sha256:...` -> store entries.
+
+    Exactly one constraint per entry, and nothing else accepted: the runtime
+    refuses a requirement it does not understand rather than ignoring it, so a
+    typo here must fail at publish rather than travel in the artefact.
+    """
+    out = []
+    for spec in specs:
+        pack, _, rest = spec.partition(":")
+        key, _, value = rest.partition("=")
+        if not pack or key not in ("atLeast", "digest") or not value:
+            raise SystemExit(
+                f"FATAL: --requires {spec!r} is not PACK:atLeast=VERSION or "
+                "PACK:digest=sha256:...")
+        out.append({"pack": pack, key: value})
+    return out
+
+
 def main():
     ap = argparse.ArgumentParser()
     ap.add_argument("--scripts", required=True, type=pathlib.Path)
@@ -240,6 +340,15 @@
                          "derived amtv4 uuid (correct for any corpus but amtv4's own)")
     ap.add_argument("--stamp", default="",
                     help="optional build stamp recorded in the store")
+    ap.add_argument("--pack-name", default="international",
+                    help="this store's identity in a report and in a merge")
+    ap.add_argument("--pack-version", default="",
+                    help="override the version taken from the corpus commit date")
+    ap.add_argument("--requires", action="append", default=[],
+                    metavar="PACK:atLeast=VERSION|digest=SHA",
+                    help="what this pack needs of another in the same merge; "
+                         "repeatable. Refused at merge with both versions named, "
+                         "rather than failing at run time on a missing macro.")
     a = ap.parse_args()
 
     root = a.manifest_root or a.scripts.parent
@@ -248,6 +357,10 @@
                                       derive_uuids=not a.no_derive_uuids)
     if a.stamp:
         store["generator"]["stamp"] = a.stamp
+    store["pack"] = pack_identity(a.pack_name, a.pack_version, root,
+                                  store["assertions"], store["prerequisites"])
+    if a.requires:
+        store["requires"] = requirements(a.requires)
 
     # A store with no assertions is never a valid outcome - it is what a wrong
     # --scripts path produces, and it is indistinguishable downstream from a
@@ -261,6 +374,11 @@
     a.out.write_text(json.dumps(store, indent=1))
     n_stmt = sum(len(v["statements"]) for v in store["assertions"].values())
     n_pre = sum(len(p["statements"]) for p in store["prerequisites"])
+    print(f"pack         {store['pack']['name']} {store['pack']['version']} "
+          f"{store['pack']['digest'][:23]} (corpus {store['pack']['corpusRef'][:12] or 'not a checkout'})")
+    for need in store.get("requires", []):
+        key = "atLeast" if "atLeast" in need else "digest"
+        print(f"requires     {need['pack']} {key} {need[key]}")
     print(f"assertions   {len(store['assertions'])}")
     print(f"statements   {n_stmt} (+{n_pre} pre-requisite)")
     print(f"knownTables  {len(store['knownTables'])}")

# Maven
target/

# Maven Release Plugin
pom.xml.tag
pom.xml.releaseBackup
pom.xml.next
release.properties

# IntelliJ IDEA
*.iml

# Ubuntu/OSX hidden files
.*


api-demo/report.csv
api-demo/tmp

*/bin/

config_local*
activemq-data/

# Whitelist some files
!.gitattributes
store
application-*.properties
snomed-release-validation-assertions
snomed-drools-rules
/local*.env

# Drools test resources are fetched from the canonical bucket by
# fetch-test-resources.sh, not committed - they are IHTSDO's to version.
/test-resources/

# Local validation job store (uploads, state and reports written at runtime)
shared-jobs/
